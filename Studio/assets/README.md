# Studio UI assets

`renegade-engine-wordmark.png` is the narrow wordmark lockup supplied in the
project owner's `Renegade_Brand_Guidelines_v1.0` document. It is cropped and
scaled for the Studio header without changing the mark, wordmark, ENGINE line
or their relative spacing.

That source document is authoritative for this logo asset only. It is not a
Studio UI specification.
