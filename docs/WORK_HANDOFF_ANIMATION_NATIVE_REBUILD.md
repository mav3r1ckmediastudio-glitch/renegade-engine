# Work handoff: replace the entire Character Importer Animations interface

Owner instruction, 23 September 2026: rebuild the ENTIRE Animations inspector from scratch as individual interactive native controls. The current dashboard is a painted presentation surface, not an acceptable functioning interface. Approved V4 is the VISUAL reference only. Do not attempt to pass off rendering, compilation, or backend tests as UI interaction success.

Repository: mav3r1ckmediastudio-glitch/renegade-engine. Local worktree: C:\Users\paulw\source\repos\renegade-complete-a6-recovery. Approved immutable-by-convention baseline: tag importer-ui-v4-approved-20260922 at 5152874 and separate local archive under Documents\Renegade-Approved-Baselines\Character-Importer-V4-20260922. Functional baseline feature/a6-animation-wiring-v4 at bc0bbed. Handoff branch feature/a6-native-importer-input-repair. Do not modify main, frozen tag or user project. Preserve unrelated untracked Tools/__pycache__/ and log.txt.

Confirmed source evidence: Studio/src/CreatorImportAnimationDashboard.h is a wi::gui::Widget whose Render() paints source cards, the orange trim bar, assignment text, headings and validation; these painted shapes have no click/drag/text handlers. Some independent Wicked buttons, dropdowns, and text fields exist in Studio/src/StudioApplication.cpp but are not integrated into a genuinely usable native inspector. The user reports that NONE of the Animations controls can be interacted with. Do not misrepresent any of them as working. A separate diagnostic build RenegadeStudio_ImporterInputProbe_v9.exe was running during handoff; do not terminate forcibly. On-disk logs BUILD/recovery/Studio/Release/importer-input-probe.log and BUILD/recovery/importer-input-live.log show Add Clip hitbox encountered but its widget state stayed IDLE on press/release. This is evidence of failed interaction, not proof of a specific underlying cause.
 do not close it forcibly or alter the user's project. The test clicked through a local test project to the Mutant importer; it did NOT validate animation-page interactions. Builds/tests that passed previously validate code and data, NOT user clicks. There is NO demonstrated functional Animations UI.

Essential implementation: replace `CreatorImportAnimationDashboard` presentation-only controls with actual native interactive widgets for EACH visible button, source card/remove action, clip row and search, clip preview transport, draggable trim handles (or clearly labelled functional numeric controls until real handles exist), editable name/start/end/speed, include/loop options, per-action dropdowns, expandable variant slots, and validation. If a control is not yet implemented, do not depict it as clickable. Preserve V4 card layout, shading and resizable inspector as a visual reference, while rebuilding actual widget structure; no decorative overlay covering hitboxes. Remove/hide legacy widgets from the Animations stage, rather than relying on focus or z-order tricks. The user wants simple and reliable interactions, not more mockup work.

Acceptance: start Studio from a NEW side-by-side executable after closing diagnostic instance normally; use a DISPOSABLE test project, never commit owner's assets during testing. Reproduce direct mouse click and keyboard typing in actual Studio; prove each visible control reacts and changes real state; check screen after interactions and scroll/resize; add automated UI input regression where possible; then perform import, persistence/reopen and A6 TestGame verification separately. Do not claim success from compiles, unit tests or graphics-backed FBX proofs alone. Do not merge to main or replace approved tag. Only ask owner to test once the independent UI acceptance has passed. Report explicit features still missing.

Relevant starting files: `Studio/src/CreatorImportAnimationDashboard.h`, `Studio/src/StudioApplication.cpp` (~lines 5230-5530 handlers; 5660-5755 registration; 7006-7124 layout; 12752-12940 visibility), `Studio/src/CreatorImportPreviewWindow.h`, Wicked `WickedEngine/WickedEngine/wiGUI.cpp` (`Window::Update`/`GUI::Update` focus ordering). Relevant existing tests `Tests/ApprovedImporterUiContract.ps1`, `Tests/CreatorAssetWorkflowGraphicsProof.cpp`, `Tests/CharacterAiAnimationTests.cpp`. Preserve the working import/animation recipe and A6 runtime underneath; only replace the broken Animations presentation/input implementation. The current branch contains temporary probe logging in `CreatorImportPreviewWindow.h` and a stage-visibility correction in `StudioApplication.cpp`. Review, remove probe logging once diagnosed, and keep the visibility correction if validated. Do not accidentally commit diagnostic logs or owner-specific absolute paths.
