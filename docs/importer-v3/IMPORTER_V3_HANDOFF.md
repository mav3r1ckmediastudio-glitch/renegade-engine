# Importer v3 — live engineering handoff

## UI fidelity programme checkpoint — 20 September 2026

- Active branch: `feature/importer-v3-native-ui-fidelity`; remote: `https://github.com/mav3r1ckmediastudio-glitch/renegade-engine.git`. The branch was created from `main` at `2c9c92195f72d868066177c5136d891860fa4873` (PR #171 merge). Latest pushed implementation checkpoint: `622c34787578f847ce2ee98349526e4da7e2badc` (`Style native importer playback strip`); draft PR: `https://github.com/mav3r1ckmediastudio-glitch/renegade-engine/pull/172`.
- Visual authority inspected locally but deliberately not committed: the approved interactive HTML and the two supplied images showing the approved shell and existing native importer. The target is a preview-first native workspace with a dark, restrained studio floor; a fixed 1.82 m mannequin; a resizable, internally scrolling inspector; and native stage/playback/navigation controls. The HTML's simulated animation is not used as an implementation source.
- Implemented in this checkpoint: authored-scene marker overlays are suppressed whenever the importer is active, preventing the stray `AUDIO` marker from appearing in the transient preview; the existing native grid is retained but uses neutral, low-contrast studio-floor values in importer mode. The existing `Studio/assets/creator-human-reference.png` and CMake copy step are already present; a fresh linked Studio visual check is still required to prove the fixed mannequin renders from the exact build.
- Changed files: `Studio/src/MarkerIconOverlay.cpp`, `Studio/src/StudioApplication.cpp`, this handoff, and root `HANDOFF.md`.
- Verification pending: source diff check, focused Windows Release build, exact native visual inspection, and the existing Character/external-animation save/reopen/Test Game acceptance. No importer persistence, retargeting, or Wicked pin behaviour was changed in this checkpoint.
- Next concrete action: create the native importer presentation shell (header/breadcrumb treatment, stage-card typography, preview controls and persistent playback/navigation strip) over the existing real widgets, then build and inspect it before proceeding to per-stage content polish.

## UI shell milestone in progress — 20 September 2026

- The native six-stage buttons have been replaced in source with importer-specific stage cards: persistent title/subtitle hierarchy, orange active state, subtle inactive cards and chevrons. The cards reuse the existing Wicked focus/click handling and existing `workspaceSection` rules; no importer workflow callbacks were replaced.
- Pending before this milestone is declared complete: source/build verification and an exact native visual check. The next shell elements are preview toolbar, persistent playback strip and bottom Previous/Next navigation; do not claim them implemented yet.

## Preview toolbar and workflow navigation milestone — 20 September 2026

- Added root-level native importer chrome, deliberately outside the right inspector's clipping/scroll area: Reset View frames the actual preview camera; `1.82 M REFERENCE` toggles the existing fixed mannequin; `+`/`−` adjust the actual preview camera FOV; Previous/Next select the existing stage model and correctly skip Rig/Animations for Model imports.
- The controls appear only while the importer is open and are hidden on Cancel and governed commit handback. No simulated animation, independent camera model, or duplicate import state was introduced.
- Pending verification: Windows compile/CI and owner visual inspection of the exact PR head. The persistent playback footer is the next committed checkpoint below.

## Native playback footer milestone — 20 September 2026

- A root-level `PLAY SELECTED CLIP` control now occupies the importer preview footer between the existing Previous/Next stage controls. It calls `PreviewSelectedCreatorImportAnimation(true, false)`, the same real native clip-preview path used by the existing Animations-stage Play control; it does not emulate the HTML animation or create a second animation system.
- It is deliberately disabled for Model imports and for Character imports with no native animation entities. It is shown only while the importer is open and is hidden again on Cancel and governed commit handback. Existing Model stage skips and Character navigation remain unchanged.
- The footer now has its own native renderer: a restrained dark strip, orange Play affordance, real selected-clip call-to-action and an explicit disabled-state explanation. Its width expands between Previous/Next without overlapping either control. It deliberately has no fabricated elapsed-time or waveform display.
- Pending verification: source compile, PR CI, a native visual check of the exact remote head, and the Character/external-animation save/reopen/Test Game acceptance. No bridge/import/persistence/retarget source or Wicked pin was changed.
- Next concrete action after this commit: compare each expanded native stage against the supplied reference screenshots and polish only controls backed by real importer data.

**Historical status below:** earlier local-branch notes are retained for provenance only. This file must be updated by Codex after every meaningful implementation checkpoint. A different assistant should read this file, inspect `git log`/`git status` and source, and continue the existing work—not restart it.

## Local branch and starting evidence

- Branch: `feature/importer-v3-local-handoff` in its own worktree (`renegade-importer-v3` under the same repos directory as `renegade-engine`). The original `main`, A2-performance and FPS-fix worktrees are separate.
- Base: `origin/main` commit `5732d8d460610045275a9149252e47f0a3d75a39` (merged #164 initial Story Flow navigation tile). Record the actual **latest commit** with `git rev-parse HEAD`; do not confuse this known base with later work.
- Pinned `WickedEngine` commit `3a800b7134aafe58461093c8abb2e274d4e64033`; pinned `ThirdParty/imnodes` commit `eb36902c892548ef94f88f51ad7e7c9c7058a71c`. Both checked out in this worktree during setup.
- Full approved v3 design: `docs/importer-v3/reference/Renegade_Importer_Interactive_Concept_v3.html`, SHA-256 `6CD5EBF25DB158B40F1FC2A98B6A91043891E5CCC81239D0CC47D9F3588189A3` (local only; ignored by Git).
- Historical handoff: `docs/importer-v3/reference/RENEGADE_NEXT_CHAT_HANDOFF_2026-09-16.md`, SHA-256 `8F65163336A8CE490BCFCF23A52EE63F5A5485799724F72BBC42ACD942D4CD00` (local only; older facts require fresh verification).
- Mission and working contract: `docs/importer-v3/CODEX_IMPLEMENTATION_BRIEF.md`; starting user instructions: root `CODEX_IMPORTER_V3_START.md`; repository instructions: root `AGENTS.md`.
- VS 18 Build Tools 2026 CMake configuration **PASSED** on this new worktree: `cmake -S . -B BUILD/renegade -G "Visual Studio 18 2026" -A x64 -DRENEGADE_EMBED_SHADERS=ON` (using verified VS18 CMake executable). The new Studio and Runtime `.vcxproj` files exist. Release compilation/link and importer acceptance are **NOT RUN**. CMake noted Gate 2C startup media absent in this checkout; that pre-existing packaging caveat is not importer validation.

## Work done / current implementation

- Local worktree created from `origin/main`, isolated from existing active branches.
- Local approved HTML and historical handoff copied byte-identically into an ignored reference directory; **not** part of the Git commit or remote repo.
- Submodules initialized at their pinned commits. Studio was observed running from the **A2 performance worktree** during setup; do not close or replace its executable without user permission.
- New importer code, a current-head Studio build, real Model `.rasset` persistence and Character retarget/playback: **NOT STARTED / NOT TESTED** at setup.

## Active priorities / first concrete action

1. Read approved v3 HTML and the full implementation brief; inspect actual `Studio/src/CreatorImportPreviewWindow.h` and `CreatorAssetWorkflowService` / `ReusableAssetService` persistence and Asset Browser reveal paths on this branch.
2. Identify how to expose the actual import failure reason in the native Studio UI and build a reproducible failure/commit test using non-private assets or approved local assets. Plain Model import was previously reported to fail to reveal `.rasset`; cause remains **unknown**.
3. Implement meaningful C++ vertical-slice progress: native v3 workspace with sole right-side stage headings and Model import wired through a real commit/reopen/reveal path. Do not stop after a plan or a static UI shell.
4. Run focused tests and build with VS18 only when safe; record actual results. Later stage: rig diagnostics and external clip playback/retarget on the model, not a CSS animation.

## Latest checkpoint (Codex must keep this section current)

- Latest completed **implementation** commit: none at setup.
- Latest handoff/setup commit: inspect `git rev-parse HEAD` after the initial documentation commit; do not invent a self-referential SHA.
- Changed implementation files: none.
- Implementation builds/tests: none. Setup-only VS18 CMake configure passed (exit 0); targeted native Studio/Runtime builds and owner importer tests have not run.
- Observed owner-acceptance result on **this importer branch**: none.
- Known failure/limitation: full importer and animation functionality have not been implemented or retested on this branch. #166/#168 contain staged importer diagnostics/roundtrip work but are separate unmerged PRs; #162 is mixed and duplicates #164 navigation work; #170 is the separate FPS change.
- Exact next command: from this worktree, run `git status --short --branch; git log -1 --oneline; git submodule status`, then read `docs/importer-v3/CODEX_IMPLEMENTATION_BRIEF.md` and follow its first vertical-slice instructions.

**Every future handoff:** replace these checkpoint entries with actual source/commit evidence and the next precise action. Commit the updates locally; record the handoff-documentation commit separately from the last tested implementation commit. No unauthorized push, CI or branch deletion.

## 18 September 2026 — current local Codex checkpoint (supersedes setup status above)

- Branch remains `feature/importer-v3-local-handoff`; starting base remains `5732d8d460610045275a9149252e47f0a3d75a39`. First implementation commit: `a3eaf4a2af24c0cd837c05eee52e06bbcbed1382` (`WIP: verify model asset commit and expose native import failures`).
- That commit changes `EngineBridge/include/renegade/bridge/{CreatorAssetWorkflowService,ReusableAssetService}.h`, `EngineBridge/src/{CreatorAssetWorkflowService,ReusableAssetService}.cpp`, `Studio/src/StudioApplication.cpp`, and `Tests/CreatorAssetWorkflowGraphicsProof.cpp`. The `.rasset` transaction must be followed by a physical document reopen, identity/hash check, current catalogue lookup, and Studio Asset Browser reveal before a success message. Failure now reports stage, destination, committed state, asset ID and reason. A post-commit failure preserves the retained source for repair. Preview starts at source scale 1.0 instead of implicit automatic normalization.
- A subsequent **uncommitted** change in `CreatorAssetWorkflowService` and the graphics proof runs the normal stable-ID placement loader against the committed product to verify serialized WISCENE/material reopening. Check `git status` before editing and commit this after validation.
- `git diff --check` passed before the first implementation commit. Initial VS18 `cmake --build BUILD/renegade --config Release --target RenegadeStudio --parallel 4` failed before compilation with MSB4184 because sandbox access to the installed Windows SDK was denied. The same command with SDK access was still building dependencies when this section was written. Record its final result and UTC time here before handoff. No fixture test or owner import has passed yet.
- The approved HTML/CSS/JS was inspected as source. Opening its local `file:` URL in the browser was blocked by browser policy, so no rendered visual inspection occurred. Current Studio still has its older combo-based pages and active-scene preview; the v3 dedicated right-inspector workspace, isolated preview, Character choice and external animation playback are not implemented. The owner's missing `.rasset` symptom has no established root cause.
- No Runtime change/test, private asset access, other worktree change, push, CI, PR, merge, rebase, reset or binary copy occurred. A separate worktree's Studio process was observed and left running.
- **Exact next action:** run `git status --short --branch; git log -2 --oneline`; finish or rerun the VS18 Release Studio build, then build/run `RenegadeCreatorAssetWorkflowGraphicsProof` with the repository's non-private LP07 FBX fixtures. After a compiled Model checkpoint, implement v3 native right-inspector navigation and isolated preview on the existing real import path. Update this section with actual results and the final handoff commit SHA (separate from implementation SHA).

## Final tested checkpoint — 18 September 2026, 13:48 UTC (supersedes the running-build notes)

- Implementation commits: `a3eaf4a2af24c0cd837c05eee52e06bbcbed1382` (container/catalogue verification and failure reporting), `64697c155c3fcc2d94aff007d41deb3a4f3bc2c6` (real serialized scene/material reopen), `ea043688ba646a2f17c636b5ef68382777109c43` (native right inspector left-edge resize). All are local on `feature/importer-v3-local-handoff`; none were pushed.
- Exact VS18 command `cmake --build BUILD/renegade --config Release --target RenegadeStudio --parallel 4`: first sandboxed attempt failed before compilation, MSB4184 on Windows SDK access. Retry with SDK access passed, exit 0, but the scene-reopen edit arrived during that build. Exact current-source rebuild `cmake --build BUILD/renegade --config Release --target RenegadeStudio RenegadeCreatorAssetWorkflowGraphicsProof --parallel 4` passed, exit 0. After the inspector resize edit, `cmake --build BUILD/renegade --config Release --target RenegadeStudio --parallel 4` passed, exit 0, producing this worktree's Release Studio executable. Known unrelated warnings include C4834 in StudioApplication/other components.
- Exact test command `ctest --test-dir BUILD/renegade -C Release -R '^RenegadeCreatorAssetWorkflowGraphicsProof$' --output-on-failure`: sandboxed attempt failed in 0.45 seconds with `destination_outside_allowed_root` from the filesystem containment check; rerunning the same test with Windows filesystem/graphics access passed 1/1 in 0.71 seconds. The proof exercises the non-private static and skinned/animated LP07 FBX fixtures, physical `.rasset`, registry/catalogue, serialized placement reopen and current Browser entry. It is **not** an owner Mutant test or proof of the approved v3 UI.
- `git diff --check` passed before each implementation checkpoint. Right inspector resizing is compiled but has **not been visually inspected in Studio**. Original Wicked Editor parity, private FBX import, Character external animation preview/retarget, complete v3 stage navigation, preview isolation, save/reopen in a user project and Runtime remain untested or unimplemented.
- **Next coding action:** inspect `Studio/src/StudioApplication.cpp` at `CreateImportScalePanel`, `ResizeLayout`, `RefreshCreatorImportWorkspaceSection`, and `CreatorImportPreviewWindow.h`. Replace the old combo stage navigation with the six approved right-inspector headings while keeping the real Model commit path, then isolate preview from the authored scene. Use a fresh native visual inspection before claiming UI parity.
- Documentation commit SHA will follow this entry; inspect `git rev-parse HEAD` after it is committed. No other worktree, active Studio process, pinned submodule or existing PR was altered.

## Owner correction received after checkpoint

The owner explicitly confirmed that **asset import is fixed**. Treat the earlier missing-`.rasset` report in this file as historical, not as a current blocker or a request to reproduce it. The owner confirmation does not identify the exact tested build, asset set, or whether it covers the unfinished v3 Character animation workflow; do not attribute the fix to a particular Codex commit without that evidence. Continue with the approved native v3 workspace, preview isolation, and Character external-animation work. The local fixture proof and its exact commands above remain valid engineering evidence.

## Approved visual reference supplied in chat

The owner attached the signed-off v3 HTML and a rendered screenshot after the previous local `file:` browser block. The attached HTML has SHA-256 `6CD5EBF25DB158B40F1FC2A98B6A91043891E5CCC81239D0CC47D9F3588189A3`, byte-identical to the ignored local reference. The screenshot was visually inspected in chat: the preview takes most of the window; a distinct fixed-width, internally scrolling right inspector contains six persistent clickable headings; only Asset Setup controls are open; Model and Character are mutually exclusive choices within that stage; a fixed 1.82 m reference stands separately from the model; a playback strip sits below the preview; navigation controls run along the bottom. The screenshot and mockup imagery are design references, not native functionality or import evidence. Never commit or upload the screenshot, embedded model image, or ignored HTML. Native Studio visual parity remains unverified.

## Continued native stage-navigation checkpoint — 18 September 2026, 14:11 UTC

- Implementation commit `690cf96877e3b99375826b0d7cb5e466780786a3` changes `Studio/src/StudioApplication.cpp`, `Studio/src/StudioApplication.h` and `Studio/src/CreatorImportPreviewWindow.h`. It removes the old section dropdown from the native importer panel, adds the six approved clickable right-inspector headings, displays only the chosen stage's controls, and positions headings around the active stage's controls. Model/Character choice is present in Asset Setup and selects `Content/Models` or `Content/Characters`. Model skips Rig and Animations. Preview lighting controls moved into Transform. Rig displays measured armature/bone counts and explicitly says humanoid mapping/external retarget preview is unverified. Existing governed Model commit/reopen/reveal path remains wired.
- Exact command: VS18 `cmake --build BUILD/renegade --config Release --target RenegadeStudio --parallel 4` with SDK access passed, exit 0, producing this worktree's Release Studio executable. `git diff --check` passed before the commit. No Runtime or bridge source changed in this checkpoint; the earlier LP07 FBX graphics proof remains the last executed importer functional test.
- **Important limitation:** no native Studio screenshot or interaction test has been obtained for this layout. Scrolling, button placement, stage spacing and alignment against the approved screenshot require visual inspection. The Character button currently selects the destination and exposes source rig/animation information; it does **not** provide external file queue, humanoid mapping validation, real retarget preview or finished Character acceptance. Do not claim v3 UI parity or Character success from the build.
- **Next coding action:** visually inspect this worktree's Studio importer with a non-private model. Repair any heading/body overlap or clipping first. Then isolate the preview scene from the authored editor scene, and implement real Character mapping/external animation queue and playback against the imported character. Keep the owner-confirmed asset import fix as accepted context; do not revive the historical missing-`.rasset` diagnosis without fresh evidence.

## Native visual and rig diagnostic checkpoint — 18 September 2026, 14:20 UTC

- Implementation commit `3d08f4e71c207c89dac3e38713cabeef18942ac5` changes only `Studio/src/StudioApplication.cpp`. On preview preparation, the existing `HumanoidRetargetService::BuildAutoHumanoidMapping` inspects the first source armature without mutating it. The Rig heading now has mapped-slot count, validity against Wicked's required bones, the exact missing required bone names, and explicit diagnostic-only status. Multi-armature input is identified as first-armature-only. This does not create a HumanoidComponent or retarget animations.
- Exact verification command: `MSBuild.exe BUILD\\renegade\\Studio\\RenegadeStudio.vcxproj /t:ClCompile /p:Configuration=Release /p:Platform=x64 /m:4 /v:minimal` using VS18 Build Tools with Windows SDK access: **passed exit 0**; existing C4834 warning at StudioApplication.cpp:11630. This compiled the changed source without linking or replacing the currently running executable. `git diff --check` passed before commit.
- Native visual inspection of the **previous** `690cf968` linked build, launched from this worktree, showed the source model in the preview and a right-side Asset Setup heading with Model/Character choice. Clicking Transform & Scale opened its real sliders; the inspector scrollbar appeared and the viewport width stayed fixed. This is a functional stage-interaction observation, not sign-off against the HTML.
- **Visible failure:** the authored level grid and an `AUDIO` marker appeared behind the model in the importer preview. The preview is not isolated from the level. The 1.82 m reference appeared as a narrow white plane in this camera view. The native layout is still much plainer than the signed-off HTML and lacks its bottom playback/navigation arrangement. No import commit, save/reopen, Character queue/playback, or Runtime test was performed in this inspection. The rig diagnostic itself was not visible because the running executable predates its commit.
- **Exact next coding action:** isolate the importer preview rendering from authored level geometry and editor overlays while preserving cancel/commit behavior, then link a new Studio build after the currently running branch executable is closed safely. Visually check the new Rig diagnostics on a skinned non-private fixture; proceed to actual Character mapping, external file queue, retarget/playback, and persisted reopen. The other worktree's Studio process, private source assets, ignored HTML/screenshot, pinned submodules, and remote remain untouched.

## ChatGPT continuation ? 18 September 2026, isolated-preview source checkpoint

- Latest implementation commit: `c9467f414597ff93ad3738b859fe4dd5b4104f86` (`WIP: render importer preview in transient scene without editing level`), on `feature/importer-v3-local-handoff`. Modified only `Studio/src/StudioApplication.cpp` and `Studio/src/CreatorImportPreviewWindow.h`; original worktrees, pinned submodules, source assets and GitHub remain untouched. This is an isolated local commit, NOT a completed importer.
- The preview model and its materials, animations, light, weather and editable transforms now live in a separately allocated Wicked scene. The native render-path scene pointer points to that preview while the workspace is open and is restored to the authored session scene on both Cancel and governed Commit. Preview placement/light no longer enters the editor command history; cancelling no longer undoes unrelated authored commands. Native editor overlays/icons and authored-scene input/ticking are suppressed during import. Right-mouse/WASD preview camera navigation is retained within the viewport left of the inspector, and original camera state is restored at exit.
- Verification: VS18 MSBuild `BUILD/renegade/Studio/RenegadeStudio.vcxproj /t:ClCompile /p:Configuration=Release /p:Platform=x64 /m:4 /v:minimal` passed exit 0; after the camera navigation change, `/t:ClCompile /p:Configuration=Release /p:Platform=x64 /p:BuildProjectReferences=false /m:4 /v:quiet` passed exit 0. Existing C4834 and MSB8029 warnings remain. `ctest --test-dir BUILD/renegade -C Release -R '^RenegadeCreatorAssetWorkflowGraphicsProof$' --output-on-failure` passed 1/1 in 0.66 seconds. `git diff --check` passed. The graphics proof exercises the *existing bridge/asset lifecycle*, NOT new preview UI, Character animation or asset-import merge integration.
- No link or native visual acceptance of `c9467f4` was performed: the owner still has this worktree's `RenegadeStudio.exe` running (observed PID 58852) and the unrelated A2 Studio process (PID 53152) also runs. Never terminate either process or overwrite a running binary without owner approval. The existing v3 executable PREDATES this isolated-preview commit. The original level grid/AUDIO contamination is observed on the prior executable, not yet confirmed resolved in the new code.
- Owner-confirmed asset-import repair was merged separately this morning; treat it as resolved owner context. This older-base isolated worktree has NOT been rebased/merged, and the exact repaired upstream commit's integration remains unverified. Do not assume this branch includes that merged repair or restart the original failure investigation.
- **Exact next action:** once the owner has closed this worktree's Studio executable or approved an independent, non-conflicting output path, link `cmake --build BUILD/renegade --config Release --target RenegadeStudio --parallel 4`, launch only that exact build using non-private fixture/project, and visually verify no level geometry, AUDIO marker or old grid leaks; inspect reference mannequin and camera/navigation, cancel/commit preserves authored scene and source identity. Repair actual failures and make a local commit. Then implement Character external FBX queue, actual humanoid retarget and playback on imported character with save/reopen and matching Runtime proof. Full native v3 UI parity, animation import/playback, owner test and Runtime remain unverified.

## ChatGPT continuation ? 18 September 2026, native Character animation source checkpoint

- Branch `feature/importer-v3-local-handoff`. New LOCAL implementation commits: `314e0276b71c04e0efb3038a80e6dd6f7106f95e` (native Play/Pause/Stop for one selected embedded clip in transient preview, validated original clip ranges), then `71975ae106c04d453a2f0763030d149045273d32` (external animation queue, opt-in humanoid source auto-mapping, real Wicked baked retarget, fingerprint checks, commit-time re-retarget). Source files in second commit: `EngineBridge/include/renegade/bridge/HumanoidRetargetService.h`, `EngineBridge/src/HumanoidRetargetService.cpp`, `Studio/src/StudioApplication.cpp`, `Tests/Phase7Gate7BHumanoidRetargetTests.cpp`. These are WIP implementation, NOT owner-accepted animation importing.
- Character Animations stage now has native Play/Pause/Stop and an external FBX/GLTF/GLB/WISCENE/VRM/VRMA picker. The queue records created native clip indices, immutable original timing ranges, source path/hash, allows removal of LAST source with native retarget Undo, and explicitly fails if required source/destination humanoid mapping or actual animation channels are missing. Embedded/retarget previews occur only in the transient Wicked scene. The normal governed CONFIRM path re-fingerprints the original external file before and after loading, re-bakes clips into the retained prepared model scene, verifies clip counts and only then proceeds to recipe/material preparation and RAsset transaction. No external private FBX was copied or staged. Queue/removal and actual playback have NOT been inspected in native Studio.
- VS18 Release proof: `cmake --build BUILD/renegade --config Release --target RenegadePhase7Gate7BTests --parallel 4` PASS exit 0, including a synthetic native Mixamo-mapped source with Wicked `Scene::RetargetAnimation(..., true, ...)` producing real self-contained keyframe data. `ctest --test-dir BUILD/renegade -C Release -R '^RenegadePhase7Gate7BHumanoidRetargetTests$' --output-on-failure` PASS 1/1 (0.04 seconds). VS18 `MSBuild.exe BUILD/renegade/Studio/RenegadeStudio.vcxproj /t:ClCompile /p:Configuration=Release /p:Platform=x64 /p:BuildProjectReferences=false /m:4 /v:quiet` PASS exit 0 on the complete source (pre-final indentation-only edit). `git diff --check` passed before both local code commits. Existing MSB8029/C4834 warnings remain. An initial combined 7B+Model CTest passed 2/2 on `314e027`; after bridge changes a combined run failed its Model proof once due Windows `journal_write_failed` / unable to remove journal file; separately rerun Model proof PASSED 1/1, including after final code checkpoint (0.61 seconds). Do not conceal the intermittent combined-run failure or claim end-to-end importer proof.
- **Not yet proven:** UI links and visuals; the v3 `RenegadeStudio.exe` remains running at observed PID 58852 and predates `c9467f4`, `314e027` and `71975ae`, so do not overwrite/terminate it without owner permission. No physical external FBX -> preview -> Character `.rasset` -> close/reopen -> playback test, no project-owned external-animation provenance on reimport, no matching Runtime/build-game acceptance. Picker/retarget happens synchronously and may stall Studio on large FBXs; removal is last-source only. Owner's asset import repair was already fixed/merged separately, but is not verified as integrated into this older-base v3 worktree. No remote push, PR, CI, worktree/submodule change or private-file publication.
- **Exact next action:** confirm `git status --short --branch; git log -3 --oneline` and v3 Studio process path. Once the owner closes ONLY the v3 worktree Studio (not an A2/other Studio) or authorizes a separate nonconflicting output, link with `cmake --build BUILD/renegade --config Release --target RenegadeStudio --parallel 4`; run this exact binary with non-private skinned source/external animation fixtures; visually verify preview isolation, rig mapping, select/play/pause/stop, external queue and error states, confirm Character RAsset and save/reopen/placement. Test matching Runtime before any acceptance claim; repair specific failures and checkpoint. Align remaining approved HTML layout, fixed 1.82 m reference, clip queue usability and source provenance. Leave user's merged asset-import fix accepted, do not re-diagnose it.

## ChatGPT continuation — 18 September 2026, Mutant head-turn root cause and scoped fix

- Owner observation: freshly imported Mutant initially faces forward, then head turns toward the side with no external clip; external animation preview also shows head flicker. Blender 4.3 background import of the retained Mutant FBX confirmed one mesh, 37 bones and one 2-frame action; head and neck transforms at frames 0/1/2/3 are effectively unchanged. No private FBX was copied into Git.
- A TEMPORARY graphics-backed native diagnostic loaded the same retained source via ImportService::PrepareModelAsset: one mesh, 37 bones, one stopped native animation. Head/neck animation translation, rotation and scale keys match their imported rest transforms to float precision. Skinning bind products for head and neck were identity within ~2.4e-7, with 2,823 vertices influenced by Head and 796 by Neck; HeadTop_End influences zero vertices. No springs, expressions or CharacterComponent. The diagnostic found the actual imported HumanoidComponent: look-at enabled, target (0,0,0), no target entity, Head slot = native mixamorig:Head. Wicked's Scene::RunProceduralAnimationUpdateSystem rotates the head each tick toward that default look-at target. Thus implicit procedural look-at is a demonstrated cause of the automatic head turn, not incorrect embedded keyframe/head mapping. The separate external-FBX playback still requires visual recheck.
- The first headless diagnostic crashed when calling full Scene::Update without Studio's complete render path; subsequent read-only diagnostics omitted full update and passed. Temporary tracked test source was restored exactly; diagnostic helpers and binary are only in ignored BUILD. Existing untracked root log.txt was left untouched.
- Fix: EngineBridge::DisableDefaultHumanoidLookAt clears LOOKAT only for an imported humanoid with no target entity and exact default zero target. Studio calls it on retained in-memory PreparedModelImport before preview clone, so preview and newly committed RAsset share the same neutral default. Explicit nonzero target or look-at entity is preserved. Synthetic 7B regression checks default, idempotency and both explicit-target cases.
- Verification: VS18 Release RenegadePhase7Gate7BTests build PASS exit 0; ctest RenegadePhase7Gate7BHumanoidRetargetTests PASS 1/1; VS18 Release Studio ClCompile PASS exit 0, existing C4834 warning. Source diff --check passes. NO updated Studio link or visual test: owner's v3 RenegadeStudio.exe was running at PID 46108. Do not terminate or overwrite the executable; link only after owner closes it. No push/merge/CI.
- Next: verify running process has closed, link exact v3 Studio and test Mutant initial T-pose and original 2-frame clip, then one external animation with stable head, Character confirm under an unused asset name, save/reopen/playback. Name-collision preflight currently logs the true reason but only shows a generic in-panel message; fix that UI feedback separately. Preview still visually exposes the authored grid/AUDIO marker; approved mockup layout and overlapping headings remain pending.

## Character import name-collision repair — 18 September 2026

- User's old v3 Studio log repeatedly reported `The selected creator asset name is already in use in the destination folder.` An existing `SourceAssets/Models/Mutant` source folder blocked a new Character with the same name even when the product destination was `Content/Characters`.
- In `CreatorAssetWorkflowService`, preflight and governed import now mirror the canonical Content destination under SourceAssets: `Content/Models` -> `SourceAssets/Models`, `Content/Characters` -> `SourceAssets/Characters`; nested destinations follow the same rule. Existing retained Model paths remain unchanged; no user project files were moved or overwritten. Duplicate names within the *same* category remain protected, with the precise conflicting project-relative file/folder reported.
- The Review & Import preflight failure now retains its exact reason in the native thumbnail/status readout and tooltip after dismissing the error dialog. The existing final commit failure dialog remains intact.
- Regression: rebuilt `RenegadeAssetBrowserTests.exe`, PASS exit 0, including shared Model/Character stem, nonmutating preflight, and protected Character duplicate. `RenegadeCreatorAssetWorkflowGraphicsProof` now imports a skinned/animated Model followed by a same-named Character in a disposable test project, verifies distinct retained-source/product paths and reopens its native animation keys. Synthetic fixture run PASS on initial implementation and PASS on retry after later change. Exact actual retained Mutant FBX run PASS exit 0 (isolated BUILD-only test root, original FBX read-only).
- Caveat: one synthetic fixture attempt failed at the SECOND asset's Windows transaction journal replace (`journal_write_failed`: unable to remove file to be replaced); immediate retry PASS, and real Mutant run PASS. This is an intermittent independent transaction-journal issue, NOT proved repaired. Investigate separately if it recurs in owner's import. Do not classify every new import failure as a name conflict; use the new exact message.
- Studio Release executable was freshly linked after v3 Studio process was confirmed closed. First full MSBuild returned failure in a post-build LUT/UI asset-copy command (`MSB3073`, exit `-1073741795`) after executable link; a repeat Studio build PASS exit 0. A manual visual import, external FBX retarget playback, complete Character persistence and Runtime acceptance are still pending; prior head-look-at fix commit `677f2fc` is included but has not been visually accepted.
- No push, merge, CI, A2 changes, user-project mutation or live-app termination. Preserve ignored/untracked root `log.txt` (preexisting). Exact executable: `BUILD/renegade/Studio/Release/RenegadeStudio.exe`.

### Transaction-journal follow-up and final local verification

- The synthetic two-asset proof also intermittently failed later at `journal_write_failed` or `commit_marker_failed`, with Windows `ReplaceFileW` error 1175 (unable to remove the replaced file). The Windows documentation states that for this specific error both named files remain in place. `ProjectDocumentTransaction.cpp` now uses the supported zero flags for `ReplaceFileW` (its prior `REPLACEFILE_WRITE_THROUGH` is documented as unsupported) and retries *only* error 1175 up to six total attempts, only when both original and replacement still exist. Other errors and missing files remain fail-closed; no destructive delete/rebuild fallback. This also protects repeated journal updates during a Character import, but is not a blanket guarantee against locks.
- After the journal fix: `RenegadeProjectDocumentTransactionTests` PASS 1/1; synthetic Creator Asset Workflow PASS five consecutive isolated runs (5/5); actual retained `Mutant.fbx` Creator Asset Workflow PASS using a disposable `BUILD/renegade/mutant-character-isolated-proof` project (original source read-only); Asset Browser tests PASS; Release Studio linked PASS exit 0, executable timestamp 18:58:47 local. The proof includes a same-named Model and Character, distinct source/product namespaces and reopened embedded animation keys. No external-animation FBX was included in the saved Character proof.
- The source category path is derived from the validated canonical *requested* `Content/...` path rather than a resolved filesystem path, preventing mirrored SourceAssets paths from following symlinks outside the intended category. No user's existing Model/Character source folders were touched or renamed.
- **Owner acceptance still required:** launch the newly linked v3 Studio and confirm a fresh `Mutant` Character import into `Content/Characters`, initial neutral T-pose after the prior look-at fix, correct external animation/head motion, saved/reopened external animation and Test Game. The existing `Mutant` Model and SourceAssets/Models/Mutant are preserved.

## 18 September 2026 — Performance diagnostic checkpoint (not a performance fix)

Owner reports ~20 s confirm-to-editor return, ~45 s frozen Character drag/place and ~13 FPS with two walking Mutants. Owner profiler screenshot: CPU frame ~76 ms, Physics ~31 ms, GPU frame ~20 ms and Animations ~0.5 ms. These are one-frame observations, not a root-cause proof. Existing terrain configuration retains static HEIGHTFIELD colliders across the generated terrain; do not change its physics or private project without measured comparison.

Local source-only instrumentation adds `[IMPORT-PERF]` backlog timings to `StudioApplication.cpp` for retarget, materials, package, confirm-to-handback and main-thread handback; `CreatorAssetDragPreview.cpp` for background placement preparation, scene instantiation, entity enumeration, full main-thread preview, command commit, and surface picks slower than 20 ms. It logs only timing and existing failure reason, no asset paths or user geometry. Existing v3 Studio is running; DO NOT link/overwrite the executable until owner closes it or authorizes a separate output. Studio Release `ClCompile` passed; no linked or visual acceptance of instrumented code yet. Actual retained Mutant FBX through the isolated creator lifecycle proof passed in ~1698 ms, which does not reproduce Studio UI time. No performance improvement is claimed. Preserve `log.txt` untracked and original project/FBX.

Next: link after process closure; measure new timing lines from Studio log after one controlled confirm and one placement in a disposable project, then separately obtain repeatable physics CPU phase measurements (Jolt step vs body setup/feedback) without disabling saved terrain or editing pinned Wicked submodule blindly. Fix only measured bottlenecks, prove no regressions in import/placement/animation/reopen, and compare owner FPS in identical scene/settings.

### Character placement performance checkpoint (18 September 2026)
- Live instrumented Mutant drag: placement preparation ~1.7 s, preview instantiate ~12 ms, full preview ~13 ms, CommandService/PlaceReusableModelCommand commit 40,006.7 ms. The commit path assigns per-entity persistent IDs and serializes the Undo snapshot.
- Local change: AssignFreshReusableHierarchyIdentities builds one set of existing IDs and enumerates scene entities once, then stamps unique IDs on the new hierarchy. Unrelated authored IDs are preserved; the Undo/Redo snapshot remains unchanged.
- A synthetic 1,190-child adoption with copied IDs colliding with an existing entity passed unique-ID, unrelated-ID and Undo/Redo checks in 7.4 ms. Four focused reusable-asset/transaction tests passed before the copied-ID test was extended; the extended instance test passed again.
- Studio executable is still the previously running instrumented build; do not relink or overwrite until Studio is closed. Real Mutant placement improvement, ~20 s importer handback and 13 FPS remain unverified/unresolved. No push/merge/CI.

### Real placement acceptance and physics timing checkpoint
- On the owner's real saved Mutant scene, before the fix, instrumented placement command commit was 40,006.7 ms; on the rebuilt `ac8b820` Studio, placement command commit was **98.2378 ms**. Background preparation was 1,142/1,556 ms and full preview 12.9038 ms in the new run. Owner reports the drag now returns quickly; the difference is an actual in-editor observation, not only a synthetic benchmark.
- Wicked Jolt has new sample-only `[IMPORT-PERF] physics` measurements of creation/refresh, properties, simulation and feedback, plus rigidbody/humanoid counts. This is diagnostic code only; no collision or simulation behaviour is changed. `WickedEngine_common` and linked reusable-instance test built successfully, and the 1,190-entity identity/Undo/Redo instance regression passed (~8 ms). The currently running Studio executable does NOT contain the physics instrumentation; do not relink while it runs.
- Remaining: use fresh Studio FPS/physics-stage measurements to isolate 13 FPS; obtain confirm-import phase timings from one deliberate import in an isolated project. Do not assert either is fixed without a real before/after. Original level, source FBX, root untracked log.txt, other worktrees and GitHub untouched.

### Isolated external-walk import benchmark and Studio handback instrumentation
- Extended the existing Creator Asset Workflow Graphics Proof with optional 4th argument `external-walk.fbx` (the default 3-argument CTest remains unchanged). In a new disposable BUILD-only project, read-only retained `Mutant.fbx` plus read-only `mutant walking.fbx` retargeted in **7.2627 ms**, the governed Character package/import/reopen took **250.978 ms**, and full lifecycle wall time was **1952 ms**. The reopened Character contained more native clips than before retarget and the normal lifecycle assertions passed. This does NOT reproduce the owner's ~20 s Studio import/handback and cannot rule out UI, material overrides, project catalogue, or a different multi-clip setup.
- Source-only extra `[IMPORT-PERF]` timings distinguish main-thread hierarchy refresh, inspector refresh and Asset Browser reveal. Studio Release `ClCompile` passed; the default Creator Asset Workflow and reusable-instance CTests passed 2/2. The currently running Studio executable predates this and the sampled Wicked physics stage diagnostics; do not overwrite it until owner closes Studio.
- Neither import wait nor 13 FPS is fixed or root-caused; preserve working project/source, unrelated worktrees and the preexisting untracked `log.txt`. No remote push, PR or CI.

### Import subphase checkpoint — 18 September 2026
- Owner's instrumented Studio import: confirm-to-handback 8797.5 ms, package 6974.6 ms, browser reveal 1577.5 ms, materials 10.7 ms, hierarchy 13.9 ms, inspector 11.3 ms. Physics samples isolate the expensive portion to Jolt simulation, not body creation/refresh; FPS root cause and fix remain unproven.
- Added diagnostic-only phase timings in ReusableAssetService (recipe/prepare, saved scene/metadata, document serialization, transaction, committed product reopen), CreatorAssetWorkflowService (core asset import, catalogue verification, reopened placement), and CreatorAssetStudioChrome (catalogue, texture metadata enrichment, card/thumbnail construction). All integrity checks remain unchanged.
- Bridge and Studio Release ClCompile PASS; Creator Asset Workflow, Reusable Instance and Project Document Transaction CTest PASS 3/3. Real retained Mutant + walk isolated proof PASS in disposable BUILD project; retarget ~1.9 ms, governed Character package ~232 ms. The original user project/FBXs were read-only and Studio's running EXE was not relinked.
- Next action: once owner closes v3 Studio, link the new executable and capture one user-approved import, then optimize only the measured specific subphase, preserving identity, transaction, catalogue verification, external animation and Undo/Redo. Do not overwrite live executable; no push/PR/CI without approval.

### Import transaction per-file profiling checkpoint (18 September 2026)

The most recent real Studio import took 6669.8456 ms from Confirm to handback. Its project transaction took 2016.1313 ms: prepare 12.0254 ms, journal 3.0346 ms, staging 978.7481 ms, commit 1020.6085 ms, cleanup 0.9266 ms. Catalogue verification took 1570.0587 ms and reopen placement 923.7142 ms. These are real-machine measurements, not a claimed speed fix.

After preserving the existing phase-timer changes in commit f10d91f, added per-document filename (basename only), byte count, staging elapsed time and commit elapsed time to the transaction timing result, logged by the reusable asset importer. The transaction's ordering, journaling, staged/committed validators, backups and recovery actions are unchanged. The first multi-worker test build failed with CL.exe access-violation error MSB6006 (-1073741819); a single-worker retry rebuilt the transaction target successfully. Focused CTests RenegadeProjectDocumentTransactionTests, RenegadeCreatorAssetWorkflowGraphicsProof and RenegadeReusableAssetInstanceTests passed (3/3; the creator proof binary was not rebuilt in this retry). The new diagnostic Studio executable has NOT been linked or deployed because Studio was running PID 56204. The user's project, assets and pre-existing root untracked log.txt were not modified; no push or merge.

Next: verify Studio is saved/closed before relinking the Release Studio executable; then have the user import once and read [IMPORT-PERF] transaction document=... timings to identify the slow destination(s). Investigate a narrowly scoped, rollback-safe I/O optimization only after measuring those per-file timings. The unrelated Jolt simulation FPS issue remains open.

### Character registry validation optimisation (19 September 2026)

- Real Studio import measured 6638.3281 ms confirm-to-handback, with AssetRegistry.renegade-assets (145614 bytes; 165 asset records, 75 imported products) spending 910.4529 ms staging and 925.6505 ms committing. The 1007039-byte Character RAsset took only 11.3658 + 20.6907 ms. This was a diagnostic measurement BEFORE this optimisation, not an observed speed improvement.
- In EngineBridge/src/ReusableAssetService.cpp only, the Character/Model governed import's registry transaction validator now compares staged/committed file bytes to the canonical JSON already generated by SerializeAssetRegistry. That serializer already runs ValidateAssetRegistry (including project ID, unique IDs and paths, dependencies, imported provenance) and generates canonical JSON. The generic ProjectDocumentTransaction still verifies bytes independently on both passes and retains journal, backup, rollback and recovery. The redundant twice-per-import DeserializeAssetRegistry + SerializeAssetRegistry calls are removed in this importer only; reimport and other writers are deliberately unchanged.
- Tests/CreatorAssetWorkflowGraphicsProof.cpp now explicitly reloads the registry after same-named Model and Character imports and verifies the original Model, its source, the Character and its source retain their IDs and project-relative paths. Single-worker Release build of six affected targets PASS. CTest: RenegadeCreatorAssetWorkflowGraphicsProof, RenegadeProjectDocumentTransactionTests, RenegadeReusableAssetInstanceTests, RenegadeReusableAssetTests, RenegadeAssetRegistryTests, RenegadeAssetRegistryPersistenceTests: PASS 6/6. git diff --check PASS.
- Only locally built tests and bridge static library changed. Owner's Studio was still running PID 50912; DO NOT relink/overwrite while running. The owner's original project, FBXs, pre-existing untracked root log.txt, other worktrees and remotes are untouched. No push, merge, PR or CI.
- Next: once owner confirms Studio closed, link exact BUILD/renegade/Studio/Release/RenegadeStudio.exe. Then capture a single Character import and compare per-file transaction staging/commit and full confirm-to-handback with the 910/926 ms and 6.638 s baseline. No live speed improvement or FPS fix claimed yet. Reimport paths and catalogue/reopen remain separate optimisation opportunities.

### 19 September 2026 — catalogue and placement subphase investigation

- Confirmed owner-side `c701211` import improvement in the real Studio log: registry document staging/commit 7.3971/20.4988 ms (was 910.4529/925.6505 ms); transaction 223.975 ms (was 2005.2317 ms); confirm-to-handback 4910.9689 ms (was 6638.3281 ms). Different imported assets and scene load prevent attributing every whole-import variation solely to the code change.
- Read-only benchmark of the owner project Content (34 folders, 325 scanned entries): AssetBrowserService folder sweep median 33.85 ms over five runs. ReadAssetRegistry of the 167-record/76-product registry took approximately 8.4–12.4 ms over repeated runs; direct BuildAssetCatalogue with that registry and metadata took 48–51 ms. These headless measurements do NOT reproduce the observed 1557 ms catalogue time inside Studio, so avoid assuming folder scans or registry parsing are the root cause.
- Added diagnostic-only [IMPORT-PERF] catalogue phases for registry, metadata, complete catalogue build and projection; placement reopen phases for registry, product validation, native scene load, texture restore and bounds. No validation, transaction, source/project, scene or persistence behavior changed.
- Focused Release build PASS; seven CTests PASS: AssetCatalogue, ReusableAsset, CreatorAssetWorkflowGraphicsProof, ReusableAssetInstance, AssetRegistry, AssetBrowser and ProjectDocumentTransaction. Temporary benchmark edits to AssetRegistryTests.cpp were restored exactly from a BUILD-only backup; ignored benchmark files stay in BUILD. Owner's original project was only read for benchmarks. Root untracked log.txt untouched.
- Linked the updated Release Studio after confirming the exact v3 executable was closed: BUILD/renegade/Studio/Release/RenegadeStudio.exe, 19 September 2026 21:40:06 local, build exit 0. No owner import has been run with this phase-timing build yet.
- The additional real retained Mutant FBX + external walking FBX lifecycle proof initially failed twice at a journal .writing file in separate disposable BUILD-only directories; the generated journal filename was 266 characters long, consistent with a Windows path-length limitation. Running the identical proof in a shorter disposable BUILD/p19a path (corresponding journal path 209 characters) PASSED, including external retarget, governed Character package, catalogue refresh/reimport and lifecycle. Do not misreport the two long-path failures as passes or as a production importer regression. Original source FBXs/project were read-only.
- Next: on one owner import capture the new catalogue and placement phase lines, then optimize the proven slow operation while preserving post-commit verification, stable IDs, texture and scene checks. This commit is diagnostic, not a speed fix; no push/merge/PR/CI.

### 19 September 2026 — live registry and editor handback measurement

- The owner imported one Character in Studio executable linked 21:40:06. Import timing: registry read within catalogue 950.1635 ms, catalogue build 667.3534 ms (total 1651.9672 ms), registry read within reopened placement 933.2988 ms (reopen total 968.0182 ms). Later creator drag preparation requests spent 4259–5184 ms inside registry reads; scene loading and textures were generally tens of milliseconds or less. Import transaction remains fast at 209.5874 ms; confirm-to-handback 10118.3753 ms versus packaging 4770.6261 ms. Do not claim that the whole duration is registry cost; scheduling/handback gap is not yet explained.
- Read-only external benchmark of the SAME 169-record/77-imported-product owner's registry WHILE Studio was open: five ReadAssetRegistry calls 8.7–9.3 ms and three BuildCatalogueSnapshot calls 50–52 ms. This rules out an inherently slow registry file or folder scan as a sufficient explanation for Studio timings. The reason inside Studio is not proven. Wicked's default modelImportWorkload_ has High priority; separate low-priority engine job threads exist, but do not assert that priority causes the delay without thread-level evidence.
- Added diagnostic-only AssetRegistryService.cpp timing of root resolution, file read, JSON parse+validation, canonical reserialization, and Windows current-thread CPU time versus steady-clock elapsed time, emitted only on reads >=50 ms. Added StudioApplication.cpp post-package-to-main-thread handback wait timing. No existing validations, transaction/rollback semantics, project data or asset behavior changed.
- Single-worker Release bridge+focused-target build PASS, seven focused CTests PASS, StudioApplication.cpp ClCompile-only PASS; the existing `[[nodiscard]]` warning at line 12128 is unrelated. Studio remains RUNNING PID 25352, so the Studio Release executable has NOT been relinked and does not contain these latest diagnostics. Original project/FBX files, untracked root log.txt, Wicked submodule, other repo and remotes untouched; no push/merge/PR/CI.
- Next: after confirming Studio is closed, link the Release RenegadeStudio.exe, rerun seven focused tests, then on one owner-approved Character import inspect `[IMPORT-PERF] registry read detailed` and `[IMPORT-PERF] post-package handback wait ms`. Use measured phase and CPU/elapsed evidence to select an optimisation; do not bypass canon, stable ID, catalogue or post-commit checks speculatively.
### 19 September 2026 - measured registry CPU bottleneck and verified snapshot fix

- Latest owner Studio import (Release EXE linked 22:04:06): 5174.6559 ms confirm-to-handback, 4834.6990 ms package, 21.3281 ms post-package handback wait. A catalogue registry read spent 976.1245 ms: path resolution 0.1754 ms, file read 1.4131 ms, parse+validate 440.0282 ms and canonical serialize+validate 534.5078 ms; Windows thread CPU 968.75 ms. Reopened placement registry read 957.1219 ms. Concurrent drag preparations repeatedly parsed and serialized the same registry, taking 4.88-5.36 s each with ~4.56-5.05 s thread CPU. The demonstrated cost is duplicate CPU work, not file I/O or a main-thread handback wait. Do not imply the 10.12 s earlier import is directly comparable across distinct scene/import runs.
- Changed ReadAssetRegistry in EngineBridge/src/AssetRegistryService.cpp: every caller still resolves the canonical document, confirms it is a regular file, and reads its complete on-disk bytes; a process-local mutex-guarded verified snapshot is reused ONLY if exact document path, expected project ID and full JSON bytes match. First read or changed bytes still undergo original DeserializeAssetRegistry validation, expected-project check, SerializeAssetRegistry canonical verification and byte comparison. Concurrent misses serialize through the same mutex instead of re-parsing identical content in parallel. Cache hit has an explicit [IMPORT-PERF] marker. No transaction, rollback, recovery, project/asset files or Wicked submodule modified.
- Tests/AssetRegistryPersistenceTests.cpp now covers repeated reads, valid external same-length byte edits, restored bytes and four concurrent readers of the changed registry. Existing wrong-project, noncanonical, interrupted-transaction recovery and staged-tampering tests remain. Single-worker Release build of affected targets PASS; eight focused CTests PASS; the newly expanded persistence test rebuilt and PASSED. Real retained Mutant FBX plus external walking FBX graphics lifecycle proof PASSED under a short disposable BUILD/renegade/p221209 directory. Original v2 project and source FBXs read-only. git diff --check PASS.
- IMPORTANT: Owner Studio is RUNNING PID 49020; existing Release EXE linked at 22:04:06 does NOT contain this cache optimisation. Do NOT overwrite/relink while running. Keep root untracked log.txt, original project/assets, other branches/remotes untouched. No push/merge/PR/CI. Next: when owner confirms Studio closed, relink BUILD/renegade/Studio/Release/RenegadeStudio.exe, run focused CTests, then one Character import to compare per-stage timings and verify cache_hit=1. This fix has no measured live speedup yet; first changed-registry read remains fully validated.
### 19 September 2026 - snapshot fix linked to Studio

- After the earlier registry-snapshot commit bff1d3b, verified that RenegadeStudio.exe had exited, then linked the exact Release executable at BUILD/renegade/Studio/Release/RenegadeStudio.exe successfully (19 September 2026 22:13:54 local, 27,573,248 bytes). Reran all eight focused Release CTests after linking: 8/8 PASS, including concurrent/externally-edited registry persistence, graphics import proof, catalogue, browser, reusable asset and transaction tests.
- Original project/FBXs were not modified. Untracked root log.txt untouched. No push, merge, PR or CI. Next: owner can reopen the usual Studio and import a Character or drag existing assets; read current Studio log for `[IMPORT-PERF] registry read detailed cache_hit=1` and full timings. The compiled snapshot is a verified fix to repeated parsing, but no new owner-side import speed has been measured with this linked EXE yet.
### 19 September 2026 - importer v3 merge-preparation checkpoint

- Owner explicitly requested preparation for a merge-ready PR independently of later visual polish. Owner tested successful native Character drag/drop and a fresh import: latest logged confirm-to-editor was 3476.6234 ms; placement preparation cache hits were 95-268 ms. This is not proof of complete UI/animation/Runtime parity.
- Fetched current `origin/main` at `8522e850` (#170 Jolt allocation fix), merged it into `feature/importer-v3-local-handoff` as `c7acb9c`, and restored the documented Wicked pin `3a800b7134aafe58461093c8abb2e274d4e64033` in commit `c499b2a`. The experimental physics-timing submodule commit `f540315` is no longer in the importer branch diff; #170 provides its separate Renegade-owned allocator overlay.
- VS18 pinned-Wicked CMake reconfigure passed, including the #170 overlay. Studio remains open and its executable was not overwritten. A matching Release `RenegadeRuntime` build has started; do not claim build/test success until its final exit code is observed.
- Existing eight focused Release tests and a real Mutant + external walk disposable-project proof passed before this reconciliation. Re-run against the reconciled source. Untracked root `log.txt` and ignored private reference files are not staged; no original owner project or FBX was changed.
- PR should remain draft until full Windows CI, matching Runtime/animation acceptance, and a brief native functional UI inspection. User's Character import and drag/drop are accepted, but full post-reopen external animation playback, unverified texture restoration and visual clipping/parity should not be claimed resolved.
- Next: finish the in-progress Runtime build; run focused tests, inspect failure output; create draft PR targeting `main` and monitor both Windows configurations. Record exact PR/head/check results in this handoff. Do not merge automatically.


## 20 September 2026 — local UI-fidelity build and GitHub CI correction

- Active remote branch: `feature/importer-v3-native-ui-fidelity`; remote: `https://github.com/mav3r1ckmediastudio-glitch/renegade-engine.git`; draft PR: `#172`. The prior remote head is `64f3c1aa6b5f9b4d19f329122ffda6e8c58ccfaa`.
- GitHub Studio Debug failed only because `MarkerIconOverlay.cpp` called `StudioRenderPath::DiagnosticImportActive()` while its declaration was private. The narrowly scoped correction moves that existing const query to the public interface in `Studio/src/StudioApplication.h`; no importer behavior, asset workflow, pinned Wicked dependency, or preview rendering logic changed.
- Local Windows Release configure passed with Visual Studio 18 CMake. The exact Release Studio build passed (exit 0) after the correction: `cmake --build BUILD/renegade --config Release --target RenegadeStudio --parallel 2`. It compiled `StudioApplication.cpp` and `MarkerIconOverlay.cpp`, then linked `BUILD/renegade/Studio/Release/RenegadeStudio.exe`.
- A separate older Studio process remains running from the protected historical importer worktree. This build used the dedicated UI-fidelity worktree and did not overwrite, relink, or terminate the running executable.
- Routine UI work remains local-build first. The failed GitHub job is an integration signal, not a blocker to local iteration; do not trigger further GitHub Actions for ordinary UI adjustments. Before integration/merge, the exact pushed PR head must receive full Windows Debug/Release CI.
- Next action: commit and push this verified visibility correction, update PR #172 with the exact SHA and CI explanation, then launch/inspect the dedicated local Release Studio build when safe. The supplied 1.82 m male FBX is still unintegrated and must not be committed publicly without the owner's explicit permission.


### 20 September 2026 — visual-pass correction in progress

- Owner rejected the first native visual pass: it remained recognisably the old importer, with a white reference block, weak camera framing, a leaked AUDIO marker and over-bright orange stage treatment. Do not describe that checkpoint as mockup fidelity.
- Source-only local correction now compiles: camera framing uses vertical and horizontal viewport axes rather than the full bounding sphere, so T-pose arm span no longer makes the character tiny; active inspector cards use the approved restrained dark selection hierarchy rather than warning-orange borders.
- Exact command: `MSBuild BUILD/renegade/Studio/RenegadeStudio.vcxproj /t:ClCompile /p:Configuration=Release /p:Platform=x64 /m:2 /v:minimal` — PASS (exit 0; pre-existing C4834 warning). The local UI-fidelity Studio instance remains open, so this change is deliberately not linked over its executable.
- Next action: replace the white billboard with a real fixed 1.82 m reference in the transient preview, remove the actual AUDIO chrome source, then close/relink only after the owner exits the dedicated UI-fidelity Studio process. Continue local-first and push each coherent checkpoint.

### 20 September 2026 — 3D reference checkpoint

- Owner supplied `Male_Reference.fbx` specifically for the importer’s fixed 1.82 m preview reference. It is committed on this public feature branch at `Studio/assets/importer/male_reference.fbx` in remote checkpoint `c2966f78654c730c5fba5957d6c498af165f5724`.
- `StudioApplication.cpp` now loads that FBX only into the transient importer preview scene, at a fixed independent scale and with both existing reference visibility controls wired to the scene entity. The old 2D white-ruler texture is intentionally no longer loaded. `Studio/CMakeLists.txt` copies the supplied FBX to `Content/importer/male_reference.fbx` beside the dedicated Studio executable.
- Local source-only Release compilation PASS: `MSBuild BUILD/renegade/Studio/RenegadeStudio.vcxproj /t:ClCompile /p:Configuration=Release /p:Platform=x64 /m:2 /v:minimal`, exit 0. The only source warning was the existing `C4834` at `StudioApplication.cpp:12439`; no executable was linked or overwritten while the dedicated UI-fidelity Studio process remains open.
- This is a code checkpoint, not visual acceptance: the exact FBX height/framing and the AUDIO marker must be checked in the freshly linked executable after the owner closes it. No CI was run.
- Next: commit/push this native-reference integration with the updated handoff, then continue the inspector shell and preview fidelity pass locally.


### 20 September 2026 - native inspector shell rebuild checkpoint

- Working directory: `renegade-importer-v3-ui-fidelity`; local branch `feature/importer-v3-native-ui-fidelity-local` based on pushed `950b92d7c6b90521a40c11b004a20b1450a646c7` (draft PR #172). This is visual presentation work on the existing functioning importer, not a new import pipeline.
- `Studio/src/CreatorImportPreviewWindow.h` now owns a noninteractive dark inspector backdrop, quiet orange header edge, divider and distinct surface for the currently expanded stage. The shell is rendered underneath existing live native controls, preserves the six stage headings, scrollable inspector and resize behavior, and has no effect on authored scene, asset identity or commit lifecycle.
- `Studio/src/StudioApplication.cpp` adds numbered step indicators and restrained active markers to stage headings, quieter title/readout label styling, theme ownership of the right panel, and active-stage body geometry. Review body gets extra room for thumbnail and cancel; the existing real Model/Character selection and animation controls remain wired.
- VS18 Release Studio `/t:ClCompile /p:BuildProjectReferences=false /m:2` passed exit 0 on these edits, 146.20 s. Existing MSB8029 and C4834 warnings only. `git diff --check` passed. Dedicated Studio was confirmed closed before starting the Release link; final link result and exact tests must be recorded below, not assumed from compile.
- Native visual acceptance against the approved HTML screenshot is not yet obtained. Confirm in the *newly linked* dedicated Studio that the 3D 1.82 m reference renders, the AUDIO marker does not leak, the numbered inspector cards and active content do not overlap after scroll/resize, and Model/Character Review/Import remain operable. Do not claim UI parity merely from a clean compile. No owner project or private assets were edited. No GitHub CI triggered for this ordinary local UI iteration.

- Release Studio link: initial compile/link reached the `RenegadeStudio.exe` output but failed during a post-build CMake asset-copy command with transient MSB3073 / Windows exit `-1073741795`; a guarded, single-worker retry succeeded (`RETRY_LINK_EXIT=0`). Verified `BUILD/renegade/Studio/Release/RenegadeStudio.exe`, 27,587,072 bytes, modified 20 September 2026 09:51:17 local. Studio was not running during either link.
- Focused tests: `RenegadeProjectDocumentTransactionTests` passed. CTest's default `RenegadeCreatorAssetWorkflowGraphicsProof` failed twice at the stale-catalogue journal staging: CTest's hardcoded 263-character transaction path exceeds classic Windows MAX_PATH, despite the system registry's LongPathsEnabled=1 (the test executable may not be long-path-aware). Re-ran the *same graphics-proof executable* with its two public LP07 fixtures and a fresh shorter output root inside ignored BUILD (`proof-ui-short`): `LP07 GATE 5 CREATOR ASSET WORKFLOW PROOF PASS`, exit 0. No source change or guard removal was used to make this pass. Four other initially selected CTest cases were Not Run because this dedicated worktree had not built their executables; do not report them as passed.
- Studio source compile and Release link are verified; **native visual parity / interactive owner acceptance still pending**. This checkpoint did not launch Studio automatically or import the owner's assets.
### 20 September 2026 - real Transform accordions and specialist-overlay isolation

- Owner's first native screenshot REJECTED visual fidelity: a cosmetically restyled shell still exposed the original ungrouped Transform sliders; resizing inspector exposed old editor chrome and an AUDIO tool. Do not claim approved-mockup parity.
- The AUDIO marker was traced to `RenegadePhysicsLabStudioChrome::Render`, which called `RenderAudioViewportTool` and other specialist renderers after its base `Render` returned early on `!IsVisible()`. Added an explicit derived visibility gate before those render calls. Import workspace now also hides the independent audio workspace and render-workspace panel and restores the prior audio visibility at exit. Visual verification is outstanding.
- Implemented five actual stateful Transform-page accordion buttons (Position, Rotation, Scale, Size & Units, Preview Lighting) using EXISTING live bound widgets; collapsed by default on new import, with controls genuinely hidden and the layout/body height recomputed after each toggle. No importer transaction, persistence, transforms or source FBX changed.
- Source-only VS18 Release `RenegadeStudio.vcxproj /t:ClCompile /p:BuildProjectReferences=false /m:2` PASS, exit 0 (~157 s). Existing MSB8029 / C4834 warnings. `git diff --check` PASS.
- Dedicated Studio remained RUNNING PID 52628. DO NOT overwrite its Release EXE or claim UI has been visually accepted. The executable currently open DOES NOT include this work. No GitHub CI, push or merge. Next: continue genuine section-specific layout improvements; after owner closes this exact Studio, link the local Release target and visually test inspector resizing, group toggles, scroll, native import and editor restoration.
### 20 September 2026 - Material and Animation page structure

- Follow-on local source changes add genuine independently collapsible Material Assignments, Texture Maps and PBR Values groups, and Animation Clips / External Animation Files groups. Each section retains its existing real selection/edit/play/file callbacks; closed groups hide their actual widgets, reflow the content, and reduce computed stage height. They reset closed on a new import; the section state persists while navigating stages. The original bulk Material/Animation labels are hidden.
- Second VS18 Release `RenegadeStudio.vcxproj /t:ClCompile /p:BuildProjectReferences=false /m:2` PASS, exit 0 (~150 s), existing MSB8029 / C4834 warnings. `git diff --check` PASS. This is source compilation only; DO NOT describe the current RUNNING Studio EXE as updated.
- Draft PR #172 unchanged; no push, merge, CI or newly linked binary. Still needs actual on-screen resize and accordion checks, dropdown clipping assessment, Studio relink after owner closes dedicated Studio, and native import transaction tests. The real approved HTML reference is absent from this dedicated worktree's ignored `docs/importer-v3/reference/` (only `.gitignore`), so do not claim exact mockup matching from a source inspection.

### 20 September 2026 17:40 BST - new accordion build linked after owner closed Studio

- Owner confirmed dedicated Studio closed; process query found no RenegadeStudio.exe before linking. Dedicated worktree HEAD at build: `6bda481b833a13a759c04fb9fcf783fef5cb1fdd` on `feature/importer-v3-native-ui-fidelity-local`; four local commits ahead of remote PR #172.
- VS18 CMake Release `RenegadeStudio` link PASS (process exit 0). Executable `BUILD/renegade/Studio/Release/RenegadeStudio.exe` updated 20 September 2026 17:40:18 local, 27,599,872 bytes. Build log: ignored `BUILD/renegade/importer-ui-relink.log`. Initial invocation of `cmake` without an absolute path did NOT run because it was absent from PATH; successful invocation used VS18's absolute `cmake.exe`.
- Post-link `ctest -C Release --output-on-failure -R '^RenegadeProjectDocumentTransactionTests$'`: 1/1 PASS, exit 0. The UI accordions, resize containment and specialist chrome bleed-through have **not yet been visually or interactively verified** in the newly linked binary; owner should reopen this exact EXE and provide screenshots with Material and Transform expanded and inspector resized both narrow/wide. No GitHub push, CI, merge, private asset edits or other-worktree changes.
