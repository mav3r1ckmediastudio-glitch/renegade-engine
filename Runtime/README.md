# Runtime

The standalone Renegade game/player executable. It does not depend on Studio
editor code.

The Phase 2 proof builds `RenegadeRuntime`, loads the same packaged WISCENE
fixture through `EngineBridge`, and renders it without linking any Studio source.
